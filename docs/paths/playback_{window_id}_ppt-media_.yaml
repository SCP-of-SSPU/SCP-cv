parameters:
  - $ref: ../components/parameters/WindowId.yaml
post:
  tags:
    - playback
  summary: 控制 PPT 当前页媒体对象
  operationId: controlPptMedia
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/PptMediaControlRequest.yaml
  responses:
    '200':
      description: PPT 媒体控制指令已持久化并接受；不表示 PowerPoint 已执行完成
      content:
        application/json:
          schema:
            $ref: ../components/schemas/PlaybackStateResponse.yaml
    '400':
      $ref: ../components/responses/ErrorResponse.yaml
