post:
  tags:
    - sources
  summary: 上传文件并创建媒体源
  description: 支持 PDF、PPT/PPS/POT/ODP、视频、音频和图片等文件。静态演示文稿会尝试转换为 PDF，动态演示文稿使用 PowerPoint。
  operationId: uploadSource
  requestBody:
    required: true
    content:
      multipart/form-data:
        schema:
          type: object
          required:
            - file
          properties:
            file:
              type: string
              format: binary
            name:
              type: string
            source_type:
              $ref: ../components/schemas/SourceType.yaml
            folder_id:
              type: integer
              nullable: true
            is_temporary:
              type: boolean
            preheat_enabled:
              type: boolean
              default: true
              description: 是否在播放器启动时预热该媒体源
  responses:
    '201':
      description: 创建成功
      content:
        application/json:
          schema:
            $ref: ../components/schemas/SourceResponse.yaml
    '400':
      $ref: ../components/responses/ErrorResponse.yaml
