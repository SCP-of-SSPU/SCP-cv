post:
  tags:
    - sources
  summary: 上传文件并创建媒体源
  description: 支持 PPT/PPS/POT/ODP、视频、音频和图片等文件。PPT 源默认使用 PowerPoint，并在导入后尝试生成播放专用 `.ppsx`/`.pps` 缓存；宏格式默认导出为非宏 `.ppsx`。
  operationId: uploadSource
  requestBody:
    required: true
    content:
      multipart/form-data:
        schema:
          type: object
          required:
            - file
          properties:
            file:
              type: string
              format: binary
            name:
              type: string
            source_type:
              $ref: ../components/schemas/SourceType.yaml
            folder_id:
              type: integer
              nullable: true
            is_temporary:
              type: boolean
            preheat_enabled:
              type: boolean
              default: true
              description: 是否在播放器启动时预热该媒体源
            ppt_backend:
              $ref: ../components/schemas/PptBackend.yaml
  responses:
    '201':
      description: 创建成功
      content:
        application/json:
          schema:
            $ref: ../components/schemas/SourceResponse.yaml
    '400':
      $ref: ../components/responses/ErrorResponse.yaml
