parameters:
  - $ref: ../components/parameters/WindowId.yaml
post:
  tags:
    - playback
  summary: 内容导航
  operationId: navigateContent
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/NavigateContentRequest.yaml
  responses:
    '200':
      description: 导航指令已持久化并接受；连续指令按队列顺序执行，最终页码通过 SSE 会话更新确认
      content:
        application/json:
          schema:
            $ref: ../components/schemas/PlaybackStateResponse.yaml
    '400':
      $ref: ../components/responses/ErrorResponse.yaml
