parameters:
  - $ref: ../components/parameters/WindowId.yaml
patch:
  tags:
    - playback
  summary: 设置循环播放状态
  operationId: toggleLoopPlayback
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/ToggleLoopRequest.yaml
  responses:
    '200':
      description: 循环设置已持久化并接受；最终播放器状态通过 SSE 会话更新确认
      content:
        application/json:
          schema:
            $ref: ../components/schemas/PlaybackStateResponse.yaml
    '400':
      $ref: ../components/responses/ErrorResponse.yaml
