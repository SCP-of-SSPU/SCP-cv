parameters:
  - $ref: ../components/parameters/WindowId.yaml
post:
  tags:
    - playback
  summary: 关闭指定窗口当前媒体源
  operationId: closeWindowSource
  responses:
    '200':
      description: 关闭指令已持久化并接受；不表示播放器已释放资源，最终状态通过 SSE 会话更新确认
      content:
        application/json:
          schema:
            $ref: ../components/schemas/PlaybackStateResponse.yaml
    '400':
      $ref: ../components/responses/ErrorResponse.yaml
