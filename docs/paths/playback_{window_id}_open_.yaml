parameters:
  - $ref: ../components/parameters/WindowId.yaml
post:
  tags:
    - playback
  summary: 打开媒体源到指定窗口
  operationId: openSourceToWindow
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/OpenSourceRequest.yaml
  responses:
    '200':
      description: 打开指令已持久化并接受；不表示播放器已执行完成，最终状态通过 SSE 会话更新确认
      content:
        application/json:
          schema:
            $ref: ../components/schemas/PlaybackStateResponse.yaml
    '400':
      $ref: ../components/responses/ErrorResponse.yaml
