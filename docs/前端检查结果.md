# 前端检查结果（2026-09-06）

## 代码基线与范围

- 采用远端 `8870228` 的规范和代码，合并提交为 `10f4004`。保留单 PowerPoint COM 放映、其余演示文稿 PDF 回退、REST/SSE 架构。
- 原本地 Broker 提交 `2a687cc` 保留在合并历史中；完整备份为 `.git/pre-broker-integration.bundle`，未提交改动备份为 `.git/pre-broker-integration-worktree.patch`。
- 浏览器使用隔离数据库、媒体目录及测试账号，后端 18000、前端 15173；没有启动播放器或 MediaMTX，没有执行设备电源、系统重启、现场音量或真实媒体播放。
- 检查视口：1280×800 桌面、768×1024 平板、390×844 手机；检查浅色、深色及跟随系统主题。

## 发现并修复

| 问题 | 复现证据 | 修复与复测 |
| --- | --- | --- |
| 仪表盘状态标签未注册 | 浏览器报告 `Failed to resolve component: n-tag`；类型检查和构建仍通过 | 补充 NTag 导入，实际渲染 3 个标签且无未解析 n-tag |
| 手机“更多”误跳首页 | 在 `/sources` 点“更多”后地址变为 `/dashboard` | 底栏先阻止默认跳转，再交父组件处理；关闭抽屉后 URL 和搜索词保持 |
| 侧边抽屉超出手机视口 | 390px 宽度下添加源抽屉宽 480px，左边界约 -90px | 添加/编辑源及编辑/预览预案采用不超过视口的宽度；手机左边界归零，表单与页脚完整显示 |
| 空闲显控提示重复 | 同一句操作提示在标题、副标题和正文出现三次 | 标题改用简短空态，隐藏无源类型副标题，正文仅保留一次提示 |
| 平板媒体表格裁切 | 768px 下 main 内容宽度溢出约 217px，操作列在裁切区，侧栏被压缩 | 表格容器支持横向滚动，侧栏禁止收缩；main 不再横向溢出，操作菜单可达；长名称卡片限制标题溢出 |

## 浏览器验证

- 登录与页面导航：仪表盘、大屏、电视左/右、媒体源、背景音乐、预案、设置。
- 设置分组：运行态、显示器、设备电源、账户、开发；连接摘要显示实际 QA 端口。
- 媒体源：长名称、离线提示、操作菜单、搜索无结果、清除搜索、添加网页的空值校验、添加/编辑抽屉取消。
- 预案：空名称校验，在隔离数据库新建全“保持”的测试预案，确认成功提示及新卡片，检查预览和关闭；未激活预案。
- 应急菜单：打开重启确认框并取消；未发送重启请求。
- 手机显控空闲态、更多菜单保留原页；深色主题切换后文字和状态可读。
- 修复后检查期间未出现新的 Vue 组件解析警告或浏览器 error 日志；旧警告保留在浏览器历史日志中，不误计为修复后的问题。

## 自动化结果

| 命令 | 结果 |
| --- | --- |
| `pnpm --prefix frontend test` | 5 项通过：Vue 模板组件绑定编译检查及导航、抽屉、空态、溢出静态回归约束 |
| `pnpm --prefix frontend run typecheck` | 通过 |
| `pnpm --prefix frontend run build` | 通过；主 JS 约 1.05 MB（gzip 约 297 KB），仍有超过 500 KB 的体积警告 |
| `uv run pytest tests/ -q --tb=short` | 420 项通过 |
| `uv run ruff check scp_cv tests` | 通过 |
| `uv run python manage.py check` | 无 error；有 staticfiles.W004：本地 static 目录不存在 |
| `uv run python manage.py makemigrations --check --dry-run` | 无模型迁移漂移 |
| `uv run python .specify/scripts/python/validate_specs.py` | 通过 |
| `git diff --check` | 通过 |

回归测试在修复前已分别失败，修复后通过；静态约束不替代真实浏览器行为验证。

## 剩余边界与启动注意

- 原业务数据库没有修改，当前停留在 playback 0025；启动最新代码前须备份数据库，再执行 `uv run python manage.py migrate`，应用 0026–0029。隔离 QA 数据库已应用这些迁移。
- 未验证 PowerPoint/VLC/直播真实播放、多屏输出、设备开关、真实媒体上传导出、PPT 动态媒体控制及长时间断线恢复。
- 实机验收继续按 `specs/002-player-runtime-reliability/quickstart.md`，T060 不标记完成。
- 本轮结论仅覆盖上述检查场景，不承诺所有设备、媒体和运行时组合绝对无 bug；未重新执行 OpenAPI 专项校验。
