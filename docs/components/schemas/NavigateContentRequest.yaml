type: object
required:
  - action
properties:
  action:
    $ref: ./NavigateAction.yaml
  target_index:
    type: integer
    description: 当 action=goto 时使用
  position_ms:
    type: integer
    description: 当 action=seek 时使用
