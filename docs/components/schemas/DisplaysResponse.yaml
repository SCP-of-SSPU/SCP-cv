allOf:
  - $ref: ./SuccessResponse.yaml
  - type: object
    required:
      - targets
      - splice_label
    properties:
      targets:
        type: array
        items:
          $ref: ./DisplayTarget.yaml
      splice_label:
        type: string
