allOf:
  - $ref: ./SuccessResponse.yaml
  - type: object
    required:
      - sessions
      - background_audio
    properties:
      sessions:
        type: array
        items:
          $ref: ./SessionSnapshot.yaml
      background_audio:
        $ref: ./BackgroundAudioSnapshot.yaml
