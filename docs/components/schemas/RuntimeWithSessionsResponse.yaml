allOf:
  - $ref: ./SuccessResponse.yaml
  - type: object
    required:
      - runtime
      - sessions
      - background_audio
    properties:
      runtime:
        $ref: ./RuntimeSnapshot.yaml
      sessions:
        type: array
        items:
          $ref: ./SessionSnapshot.yaml
      background_audio:
        $ref: ./BackgroundAudioSnapshot.yaml
