allOf:
  - $ref: ./SuccessResponse.yaml
  - type: object
    required:
      - sessions
      - detail
    properties:
      sessions:
        type: array
        items:
          $ref: ./SessionSnapshot.yaml
      detail:
        type: string
        example: 系统关闭请求已发送
