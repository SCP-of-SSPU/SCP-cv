allOf:
  - $ref: ./SuccessResponse.yaml
  - type: object
    required:
      - sessions
      - detail
      - commands
    properties:
      sessions:
        type: array
        items:
          $ref: ./SessionSnapshot.yaml
      detail:
        type: string
        example: 系统关闭请求已发送
      commands:
        type: array
        description: 关闭窗口及应用运行时音频策略时实际持久化的命令
        items:
          $ref: ./ControlCommandState.yaml
