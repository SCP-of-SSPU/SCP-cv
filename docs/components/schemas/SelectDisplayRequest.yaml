type: object
properties:
  window_id:
    type: integer
    minimum: 1
    maximum: 4
    default: 1
  display_mode:
    $ref: ./DisplayMode.yaml
  target_label:
    type: string
    description: 单屏模式可省略，省略时由后端选择默认显示器
