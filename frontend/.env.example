# SCP-cv 前端开发期环境变量
# 复制为 frontend/.env 后按实际部署修改；该文件不会被提交。

# Vite 开发服务器监听端口（host 默认 0.0.0.0）
VITE_FRONTEND_PORT=5173

# 后端 Django REST/SSE 服务直连地址（含 scheme 与端口，无尾随斜杠）
VITE_BACKEND_TARGET=http://127.0.0.1:8000
